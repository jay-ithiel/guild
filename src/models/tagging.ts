class Tagging {
    public tagId: number;
    public blogId: number;

    constructor(
        tagId: number,
        blogId: number
    ) {
        this.tagId = this.tagId;
        this.blogId = this.blogId;
    }
}

export default Tagging;
