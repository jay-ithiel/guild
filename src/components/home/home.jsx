import React from 'react';

import Feed from '../feed/feed';
import Blogs from '../blogs/blogs';

const Home = () => (
  <section id='home'>
    <Feed/>
    <Blogs/>
  </section>
);

export default Home;
